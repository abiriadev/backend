in: query
name: location
description: |
  현재 사용자의 위치를 전달합니다.

  `sortBy=location` 으로 거리 정렬을 할 때 이 옵션으로 현재의 위치를 반드시 전달해야 합니다.

  `sortBy`를 `location`로 지정하지 않았다면 이 파라미터는 아무 역할도 하지 않습니다.
schema:
  $ref: ../schemas/LocationTuple.yaml
explode: false
